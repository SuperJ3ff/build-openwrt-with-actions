name: 🚀 编译 qmimmortalwrt7981(Build qmimmortalwrt7981)

on:
  workflow_dispatch:
    inputs:
      owner:
        description: "openWRT 仓库的拥有者 (Owner of openWRT repo)"
        type: string
        required: true
        default: "padavanonly"
      repo:
        description: "openWRT 仓库的名字 (Name of openWRT repo)"
        type: string
        required: true
        default: "immortalwrt-mt798x-6.6"
      branch:
        description: "openWRT 仓库的分支 (Branch of openWRT repo)"
        type: string
        required: true
        default: "openwrt-24.10-6.6"
      multithreading:
        description: "开启多线程编译 (Enable multithreading build)"
        type: boolean
        default: true
      ssh:
        description: "使用 ssh 连接到编译环境 (Connect to the build environment using ssh)"
        type: boolean
        default: false
      isFiles:
        description: "使用files大法 (Whether to upload the etc files)"
        type: boolean
        default: false

jobs:
  build:
    runs-on: ubuntu-latest
    name: 🚀 编译 (Build)
    steps:
      - name: Before freeing up disk space
        run: |
          echo "Before freeing up disk space"
          echo "=============================================================================="
          df -hT
          echo "=============================================================================="

      - name: "Optimize Disk Space"
        uses: "hugoalh/disk-space-optimizer-ghaction@v0.8.1"
        with:
          operate_sudo: "True"
          general_include: ".+"
          general_exclude: |-
            ^GCC$
            ^G\+\+$
            Clang
            LLVM
          docker_include: ".+"
          docker_prune: "True"
          docker_clean: "True"
          apt_prune: "True"
          apt_clean: "True"
          homebrew_prune: "True"
          homebrew_clean: "True"
          npm_prune: "True"
          npm_clean: "True"
          os_swap: "True"

      - name: Freeing up disk space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 3072
          swap-size-mb: 1
          remove-dotnet: "true"
          remove-android: "true"
          remove-haskell: "true"
          remove-codeql: "true"
          remove-docker-images: "true"

      - name: Free up disk space complete
        run: |
          echo "Free up disk space complete"
          echo "=============================================================================="
          df -hT
          echo "=============================================================================="

      - name: 下载编译所需的依赖 (Install the software packages required for compilation)
        run: |
          export TMPDIR=$RUNNER_TEMP
          sudo mkdir -p $TMPDIR
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo rm -rf /var/cache/apt/*
          sudo apt-get update -y
          sudo apt-get install -y --no-install-recommends ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential \
            bzip2 ccache clang cmake cpio curl device-tree-compiler ecj fastjar flex gawk gettext gcc-multilib \
            g++-multilib git gnutls-dev gperf haveged help2man intltool lib32gcc-s1 libc6-dev-i386 libelf-dev \
            libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses-dev libpython3-dev \
            libreadline-dev libssl-dev libtool libyaml-dev libz-dev lld llvm lrzsz mkisofs msmtp nano \
            ninja-build p7zip p7zip-full patch pkgconf python3 python3-pip python3-ply python3-docutils \
            python3-pyelftools qemu-utils re2c rsync scons squashfs-tools subversion swig texinfo uglifyjs \
            upx-ucl unzip vim wget xmlto xxd zlib1g-dev zstd
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/* /var/cache/apt/*

      - name: 克隆当前仓库 (Clone current repo)
        uses: actions/checkout@v4

      - name: 克隆 openWRT 仓库 (Clone openWRT repo)
        uses: actions/checkout@v4
        with:
          repository: "${{inputs.owner}}/${{inputs.repo}}"
          ref: ${{inputs.branch}}
          path: openWRT

      - name: 复制当前仓库的配置文件到 openWRT 仓库  (Copy current repo config to openWRT repo)
        run: cp -f .config diy.sh openWRT/

      - name: 如果选择了上传 files 大法，则复制 files 目录到 openWRT 仓库 (If you choose to upload the etc files, copy the etc directory to the openWRT repo)
        if: ${{inputs.isFiles == true}}
        run: cp -r files openWRT/files

      - name: 运行 diy.sh (Run diy.sh)
        working-directory: openWRT
        run: |
          chmod +x diy.sh
          ./diy.sh

      - name: 更新并下载 feeds (Update and download feeds)
        working-directory: openWRT
        run: ./scripts/feeds update -a && ./scripts/feeds install -a

      - name: 使用 MT7981 默认配置覆盖 .config (Use MT7981 defconfig)
        working-directory: openWRT
        run: cp -f defconfig/mt7981-ax3000.config .config

      # ========== 新增：生成完整配置并单独编译 qmodem ==========
      - name: Generate full .config for qmodem debug
        working-directory: openWRT
        run: make defconfig

      - name: Compile qmodem only and capture logs
        working-directory: openWRT
        id: compile_qmodem
        continue-on-error: true
        run: |
          make package/qmodem/compile V=s 2>&1 | tee qmodem_build.log
          echo "exit_code=$?" >> $GITHUB_OUTPUT

      - name: Upload qmodem build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: qmodem-build-log
          path: openWRT/qmodem_build.log

      - name: Check qmodem compile status
        if: steps.compile_qmodem.outputs.exit_code != '0'
        run: |
          echo "❌ qmodem 编译失败，请下载 artifact 'qmodem-build-log' 查看详细错误"
          exit 1
      # ========================================================

      # 原来的完整编译步骤（已注释，如需恢复请取消注释）
      # - name: 多线程编译 openWRT (Build openWRT multithreading)
      #   if: ${{inputs.multithreading == true}}
      #   working-directory: openWRT
      #   run: make -j$(nproc) V=s
      #
      # - name: 单线程编译 openWRT (Build openWRT single thread)
      #   if: ${{inputs.multithreading == false}}
      #   working-directory: openWRT
      #   run: make -j1 V=s
      #
      # - name: 上传编译结果到 Artifact (Upload build result to Artifact)
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: openWRT-build-result
      #     path: openWRT/bin/targets
